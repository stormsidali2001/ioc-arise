---
title: Getting Started
description: Quick start guide for ioc-arise
---

## Installation

```bash
npm install -g @notjustcoders/ioc-arise
```

## Usage

1. **Generate IoC Container**

```bash
ioc-arise generate
```

2. **Use the Container**

```typescript
import { inject } from "./container.gen";

const userService = inject('coreModule.UserService');
```

### Modules Configuration

Organize code into separate modules:

```json
{
  "source": "./src",
  "output": "./src/container.ts",
  "modules": {
    "UserModule": ["user/**"],
    "TodoModule": ["todo/**"]
  }
}
```

Access: `inject('userModule.UserService')`

### Scopes

- **Singleton** (default): One instance per container
- **Transient**: New instance each time

```typescript
/**
 * @scope transient
 */
export class TempService {}
```

Then run:

```bash
npx @notjustcoders/ioc-arise generate
```

---

**Next Steps:** Check out our [examples](/examples/) for more advanced usage patterns.
